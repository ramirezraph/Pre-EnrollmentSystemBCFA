@model EnrollBCFA.Models.tblSchoolYear

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="view-section">
    <div class="acad-header">
        <h3 id="lblAcad">Delete Academic Year</h3>
    </div>
    <div class="acad-container" style="color: lightgray; padding-left: 3%;">
        <h3 class="text-danger">Are you sure you want to delete this?</h3>
        <div>
            <br />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.SchoolYear)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.SchoolYear)
                </dd>
                <dt>
                    @Html.DisplayNameFor(model => model.status)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.status)
                </dd>

            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                <div class="form-actions no-color">
                    <div class="row">
                        <div class="col-md-12">
                            <input type="submit" value="Yes, please delete." class="btn btn-danger btnSaveEditAcad" style="margin-left: 19%;" />
                        </div>
                        <div class="col-md-12">
                            <a href="@Url.Action("Index", "SchoolYears")" class="btnBackToList">&#8592; Back to list</a>
                        </div>
                    </div>
                </div>
            }
        </div>

    </div>
</div>

